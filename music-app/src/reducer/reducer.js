import {useReducer} from "react";

export const initState = {
  musicList: [

  ],
  filter: []
}

const reducer = (state, action) => {
  switch (action.type) {
    case "value":
      break
    default:
      break;
  }
}
export default reducer